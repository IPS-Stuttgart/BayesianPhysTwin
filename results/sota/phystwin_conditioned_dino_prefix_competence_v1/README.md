# Conditioned DINO prefix competence evidence

This directory contains the source report, target-free prediction archive,
prediction report, prediction seal, and post-seal prefix evaluation for the
frozen one-case source control.

The withheld manual prefix target is intentionally not included.

See `docs/phystwin_conditioned_dino_prefix_result.md` for the result and claim
boundary.
