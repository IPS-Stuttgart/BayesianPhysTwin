# Recursive-corruption v2 change summary

- Adds a deterministic residual comparator with the identical guard used by the
  Gaussian recursive arm.
- Adds five imperfect-provider-cue conditions.
- Uses a fresh 200-domain evidence roster with varying dynamics and actions.
- Retains per-time-step traces for exact recovery-curve regeneration.
- Restricts primary status to two preregistered endpoints.
- Computes uncertainty at the seed-domain level after reducing nested
  conditions.
- Represents unsupported metrics as JSON `null`.
- Preserves v1 unchanged and opens no protected physical outcome.
