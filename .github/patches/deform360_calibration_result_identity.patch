diff --git a/scripts/science/inventory_deform360_calibration_prepared_source.py b/scripts/science/inventory_deform360_calibration_prepared_source.py
index cefba3d..0000000 100755
--- a/scripts/science/inventory_deform360_calibration_prepared_source.py
+++ b/scripts/science/inventory_deform360_calibration_prepared_source.py
@@ -38,8 +38,8 @@ from bayesian_phystwin.deform360_calibration_source_run_record import (
 )
 
 INVENTORY_SCHEMA = "bayesian-phystwin.deform360-calibration-prepared-source-inventory"
-INVENTORY_VERSION = 1
-INVENTORY_SEMANTICS = "exact-retained-calibration-rgb-tactile-robot-inventory-v1"
+INVENTORY_VERSION = 2
+INVENTORY_SEMANTICS = "exact-retained-calibration-rgb-tactile-robot-inventory-v2"
 INVENTORY_STATUS = "complete-calibration-only-prepared-source"
 INVENTORY_CLAIM_BOUNDARY = (
     "Calibration-only retained-source custody and portable array/media contracts. "
@@ -449,6 +449,10 @@ def build_inventory(
     if result.get("result_valid") is not True:
         raise ValueError("prepared-source result is invalid")
     _compare_summary(result, record, keys=_RESULT_KEYS, name="result")
+    result_sha256 = sha256_digest(
+        result.get("result_sha256"),
+        name="calibration source result semantic SHA-256",
+    )
     result_value, result_file_sha256 = load_json_object(result_path)
     if result_file_sha256 != result.get("result_file_sha256"):
         raise ValueError("prepared-source result changed after validation")
@@ -626,6 +630,7 @@ def build_inventory(
             record.get("record_sha256"),
             name="calibration source record SHA-256",
         ),
+        "calibration_source_result_sha256": result_sha256,
         "object_count": len(objects),
         "objects": objects,
         "source_artifacts": source_artifacts,
diff --git a/src/bayesian_phystwin/deform360_calibration_visual_execution_admission.py b/src/bayesian_phystwin/deform360_calibration_visual_execution_admission.py
index df6df0b..0000000 100644
--- a/src/bayesian_phystwin/deform360_calibration_visual_execution_admission.py
+++ b/src/bayesian_phystwin/deform360_calibration_visual_execution_admission.py
@@ -44,9 +44,9 @@ DEFORM360_CALIBRATION_VISUAL_EXECUTION_ADMISSION_SEMANTICS: Final = (
 DEFORM360_PREPARED_SOURCE_INVENTORY_SCHEMA: Final = (
     "bayesian-phystwin.deform360-calibration-prepared-source-inventory"
 )
-DEFORM360_PREPARED_SOURCE_INVENTORY_VERSION: Final = 1
+DEFORM360_PREPARED_SOURCE_INVENTORY_VERSION: Final = 2
 DEFORM360_PREPARED_SOURCE_INVENTORY_SEMANTICS: Final = (
-    "exact-retained-calibration-rgb-tactile-robot-inventory-v1"
+    "exact-retained-calibration-rgb-tactile-robot-inventory-v2"
 )
 DEFORM360_PREPARED_SOURCE_INVENTORY_STATUS: Final = (
     "complete-calibration-only-prepared-source"
@@ -99,6 +99,7 @@ _INVENTORY_FIELDS = frozenset(
         "selection_artifact_sha256",
         "visual_provider_lock_id",
         "calibration_source_run_record_sha256",
+        "calibration_source_result_sha256",
         "object_count",
         "objects",
         "source_artifacts",
@@ -474,6 +475,7 @@ def validate_deform360_prepared_source_inventory(
         "selection_artifact_sha256",
         "visual_provider_lock_id",
         "calibration_source_run_record_sha256",
+        "calibration_source_result_sha256",
     ):
         sha256_digest(inventory[field], name=field)
     if inventory["information_boundary"] != _INVENTORY_INFORMATION_BOUNDARY:
@@ -796,11 +798,15 @@ def build_deform360_calibration_visual_execution_admission(
         if plan[field] != inventory[field]:
             raise ValueError(f"plan and inventory differ: {field}")
     inventory_sources = cast(Mapping[str, Any], inventory["source_artifacts"])
-    result_sha256 = sha256_digest(
+    sha256_digest(
         inventory_sources["sources/calibration-source/result.json"],
-        name="inventory calibration-source result",
+        name="inventory calibration-source result file",
+    )
+    result_sha256 = sha256_digest(
+        inventory["calibration_source_result_sha256"],
+        name="inventory calibration-source result semantic identity",
     )
     if plan["calibration_source_result_sha256"] != result_sha256:
         raise ValueError("plan and inventory differ: calibration-source result")
diff --git a/tests/test_deform360_calibration_visual_execution_admission.py b/tests/test_deform360_calibration_visual_execution_admission.py
index af7f7f3..0000000 100644
--- a/tests/test_deform360_calibration_visual_execution_admission.py
+++ b/tests/test_deform360_calibration_visual_execution_admission.py
@@ -91,8 +91,8 @@ def _inventory(plan: dict[str, object]) -> dict[str, object]:
     identity: dict[str, object] = {
         "schema": ("bayesian-phystwin.deform360-calibration-prepared-source-inventory"),
-        "schema_version": 1,
-        "semantics": "exact-retained-calibration-rgb-tactile-robot-inventory-v1",
+        "schema_version": 2,
+        "semantics": "exact-retained-calibration-rgb-tactile-robot-inventory-v2",
         "status": "complete-calibration-only-prepared-source",
         "implementation_revision": "8" * 40,
         "calibration_source_revision": "7" * 40,
@@ -104,11 +104,10 @@ def _inventory(plan: dict[str, object]) -> dict[str, object]:
         "calibration_source_run_record_sha256": plan[
             "calibration_source_run_record_sha256"
         ],
+        "calibration_source_result_sha256": plan[
+            "calibration_source_result_sha256"
+        ],
         "object_count": 10,
         "objects": objects,
         "source_artifacts": {
-            "sources/calibration-source/result.json": plan[
-                "calibration_source_result_sha256"
-            ],
+            "sources/calibration-source/result.json": _digest("result-file"),
             "sources/stage0/selection.json": plan["selection_artifact_sha256"],
         },
         "information_boundary": {
@@ -169,6 +168,24 @@ def test_admission_binds_all_jobs_to_exact_source_bytes(tmp_path: Path) -> None:
     }
 
 
+def test_admission_binds_semantic_and_file_result_identities_separately(
+    tmp_path: Path,
+) -> None:
+    plan_path, inventory_path, plan = _inputs(tmp_path)
+    inventory = json.loads(inventory_path.read_text(encoding="utf-8"))
+    result_file_sha256 = inventory["source_artifacts"][
+        "sources/calibration-source/result.json"
+    ]
+    assert result_file_sha256 != plan["calibration_source_result_sha256"]
+
+    admission = build_deform360_calibration_visual_execution_admission(
+        visual_production_plan_path=plan_path,
+        prepared_source_inventory_path=inventory_path,
+        implementation_revision=IMPLEMENTATION_REVISION,
+    )
+
+    assert admission["calibration_source_result_sha256"] == plan[
+        "calibration_source_result_sha256"
+    ]
+
+
 def test_admission_round_trip_is_deterministic_and_non_replacing(
     tmp_path: Path,
 ) -> None:
@@ -280,6 +297,20 @@ def test_frame_and_upstream_identity_mismatch_fail_closed(tmp_path: Path) -> None:
             implementation_revision=IMPLEMENTATION_REVISION,
         )
 
+    plan_path, inventory_path, _plan = _inputs(tmp_path / "result")
+    inventory = json.loads(inventory_path.read_text(encoding="utf-8"))
+    inventory["calibration_source_result_sha256"] = "d" * 64
+    _rewrite_inventory(inventory_path, inventory)
+    with pytest.raises(ValueError, match="calibration-source result"):
+        build_deform360_calibration_visual_execution_admission(
+            visual_production_plan_path=plan_path,
+            prepared_source_inventory_path=inventory_path,
+            implementation_revision=IMPLEMENTATION_REVISION,
+        )
+
 
 def test_duplicate_keys_boolean_versions_and_tampering_are_rejected(
     tmp_path: Path,
diff --git a/tests/test_deform360_calibration_prepared_inventory.py b/tests/test_deform360_calibration_prepared_inventory.py
index eda3bc7..0000000 100644
--- a/tests/test_deform360_calibration_prepared_inventory.py
+++ b/tests/test_deform360_calibration_prepared_inventory.py
@@ -214,6 +214,17 @@ def test_inventory_binds_all_prepared_bytes_without_local_paths(tmp_path: Path) -> None:
     value = json.loads(output.read_text(encoding="utf-8"))
 
+    result_file_sha256 = value["source_artifacts"][
+        "sources/calibration-source/result.json"
+    ]
+    assert value["schema_version"] == 2
+    assert value["semantics"] == (
+        "exact-retained-calibration-rgb-tactile-robot-inventory-v2"
+    )
+    assert value["calibration_source_result_sha256"] == inputs.chain.result[
+        "result_sha256"
+    ]
+    assert result_file_sha256 == _sha256(inputs.chain.result_path)
+    assert result_file_sha256 != value["calibration_source_result_sha256"]
     assert value["object_count"] == 10
     assert len(value["objects"]) == 10
     assert value["information_boundary"]["confirmation_payloads_opened"] is False
diff --git a/docs/deform360_calibration_prepared_inventory.md b/docs/deform360_calibration_prepared_inventory.md
index bfd2b9e..0000000 100644
--- a/docs/deform360_calibration_prepared_inventory.md
+++ b/docs/deform360_calibration_prepared_inventory.md
@@ -139,8 +139,13 @@ Publication is atomic and refuses overwrite.
 ## Output and downstream use
 
 The content-addressed inventory contains one row per exact calibration object,
-the exact source artifact digests, the action-selected window, all camera media
-contracts, tactile array contracts, robot array contracts, and the closed
-information boundary.
+the exact source-file digests, the semantic calibration-source result identity,
+the action-selected window, all camera media contracts, tactile array contracts,
+robot array contracts, and the closed information boundary. Inventory schema
+version 2 deliberately carries both the result file digest and the result's
+internal `result_sha256`; they are different identities and neither substitutes
+for the other.
 
 The frozen visual-production plan and execution admission then bind every
 admitted camera job to an inventoried video and timestamp path, SHA-256, byte
diff --git a/.github/workflows/launch-deform360-calibration-retained-source-once.yml b/.github/workflows/launch-deform360-calibration-retained-source-once.yml
index e2c8c63..0000000 100644
--- a/.github/workflows/launch-deform360-calibration-retained-source-once.yml
+++ b/.github/workflows/launch-deform360-calibration-retained-source-once.yml
@@ -83,4 +83,4 @@ jobs:
             "repos/${GITHUB_REPOSITORY}/issues/148/comments" \
             -f body="${body}"
 
-# Reviewed retained-source admission request: 2026-08-09-setup-python-v3
+# Reviewed retained-source admission request: 2026-08-09-result-identity-v4
