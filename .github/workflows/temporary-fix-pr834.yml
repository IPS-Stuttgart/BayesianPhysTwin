name: Temporary repair for same-mean covariance audit

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/temporary-fix-pr834.yml"
      - ".github/workflows/deform360-same-mean-query-covariance.yml"
      - "scripts/**"
      - "src/**"
      - "tests/**"

permissions:
  contents: write

jobs:
  repair:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.head_ref == 'science/deform360-same-mean-query-covariance-v6'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out pull-request branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
          persist-credentials: true
          clean: true
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - name: Install formatter
        run: python -m pip install 'ruff>=0.15.22,<0.17'
      - name: Repair changed Python formatting
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin main:refs/remotes/origin/main
          mapfile -t files < <(git diff --name-only --diff-filter=ACMR origin/main...HEAD -- '*.py')
          if (( ${#files[@]} )); then
            python -m ruff format "${files[@]}"
            python -m ruff check --fix "${files[@]}"
          fi
      - name: Commit canonical repair
        shell: bash
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          set -euo pipefail
          git add -- '*.py'
          if git diff --cached --quiet; then
            echo 'No formatting repair needed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Apply canonical formatting to same-mean covariance audit'
          git push origin "HEAD:${HEAD_REF}"
