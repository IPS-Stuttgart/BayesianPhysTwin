name: Temporary exact-source export

on:
  push:
    branches: [agent/temporary-export-current-source-20260806]
    paths:
      - ".github/workflows/temporary-export-current-source.yml"
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/temporary-export-current-source.yml"

permissions:
  contents: read

concurrency:
  group: temporary-exact-source-export-${{ github.ref }}
  cancel-in-progress: true

jobs:
  export:
    if: >-
      github.event_name == 'push' ||
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out exact source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
          clean: true

      - name: Archive exact public checkout
        shell: bash
        run: |
          set -euo pipefail
          expected_sha="${{ github.event.pull_request.head.sha || github.sha }}"
          test "$(git rev-parse HEAD)" = "${expected_sha}"
          archive="${RUNNER_TEMP}/bayesian-phystwin-source-${expected_sha}.tar.gz"
          tar \
            --exclude=.git \
            --exclude='*.pyc' \
            --exclude='__pycache__' \
            -czf "${archive}" .
          sha256sum "${archive}" > "${archive}.sha256"
          {
            echo "SOURCE_ARCHIVE=${archive}"
            echo "SOURCE_CHECKSUM=${archive}.sha256"
          } >> "${GITHUB_ENV}"

      - name: Upload exact source archive
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: bayesian-phystwin-source-${{ github.event.pull_request.head.sha || github.sha }}
          path: |
            ${{ env.SOURCE_ARCHIVE }}
            ${{ env.SOURCE_CHECKSUM }}
          if-no-files-found: error
          retention-days: 1
