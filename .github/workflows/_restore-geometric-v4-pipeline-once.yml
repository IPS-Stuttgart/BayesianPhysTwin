name: Retired geometric v4 restore helper

on:
  push:
    branches:
      - agent/materialize-deform360-joint-sparse-geometric-v4
    paths:
      - .github/workflows/_restore-geometric-v4-pipeline-once.yml

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "This one-time helper is retired and performs no write."
