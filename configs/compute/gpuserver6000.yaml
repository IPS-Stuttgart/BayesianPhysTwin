host: gpuserver6000
role: larger GPU experiments
default_run_root: runs/gpuserver6000
notes:
  - Use for PhysTwin reconstruction, replay, and MPC-scale jobs.
  - Keep raw outputs on the server unless they are curated for the paper repo.

