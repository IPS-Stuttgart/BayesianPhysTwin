# Deform360 v6.1 endpoint gsplat base-version repair

The sole gsplat-attestation successor, GitHub Actions run `31666009357` at
protected-main revision `cde6f9da37c2697e50897f2b9ce8360c744325bc`, passed both
target-closed contract jobs, built and attested the exact isolated CUDA runtime,
and admitted its one empty durable source root. It then stopped in both endpoint
workers before endpoint geometry materialization because the endpoint dependency
validator reported `gsplat distribution changed`.

The run is terminal and will not be retried. Its compact technical-failure receipt
has ID `69967a3441da85fc1a862d8206b6cb750d167dbabfc0c921c47adbc87f738f61`
and file SHA-256
`a0b6b2ff1a7ce5dd4732a2896b0f6f4cff593a5f29e43b9bd5a6cc160135c027`.
The source-suffix authorization marker was opened, and public video metadata was
checked, but no endpoint geometry, source score, support report, source-gate
decision, confirmation payload, target outcome, or held-v8 artifact was produced
or inspected. The endpoint root is empty, both workers returned one, and the
registered source gate remains unevaluated.

The frozen source amendment stores an untagged gsplat release base (`1.4.0`) and
the exact tagged wheel identity (`1.4.0+pt24cu121`). The endpoint validator
incorrectly compared the full module version with the base field. The successor
requires the module and distribution versions to equal the exact tagged identity
and separately derives the release base by removing the local build tag.

This changes only endpoint dependency attestation. It retains the exact candidate,
100 sealed predictions, cohort, source plan, wheel bytes, compiled extension,
Torch/CUDA/cuDNN runtime, selector, reconstruction algorithm, scorer, covariance,
metrics, source gate, fallback, and information boundary. The old workflow is
disabled and may not be rerun; any successor execution uses a fresh workflow
identity and durable namespace after reviewed protected-main merge.

This is implementation evidence only. It authorizes no independent confirmation,
target access, claim, deployment, or state-of-the-art statement. The experiment
continues to use public real-world Deform360 recordings, collect no new
measurements, require no human approval, exclude decoded-uniform Prob4D fusion,
and keep confirmation closed unless the unchanged registered source gate passes.
