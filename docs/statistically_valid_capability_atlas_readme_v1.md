# Statistically valid capability atlas handoff

The exact capability atlas and the cross-fitted trajectory-conformal envelope are source-compatible. The next implementation step is to expose their composition as a public API and test that the calibrated half-space offsets equal the model-side offsets minus the nonnegative object-level correction.
