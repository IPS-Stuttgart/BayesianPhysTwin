# Continuous task-family score

For a realized affine pairwise loss gap and the atlas's max-affine model envelope, the calibration residual is concave piecewise affine. Its maximum over a registered task rectangle is solved as a small linear program by exhaustive vertex enumeration under a strict complexity cap. Evaluating only box corners is insufficient because the maximum can occur at an interior envelope kink.
