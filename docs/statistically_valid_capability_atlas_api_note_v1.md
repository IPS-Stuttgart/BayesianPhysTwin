# API publication note

The implementation is prepared locally as `statistically_valid_capability_atlas_v1.py` with a focused test module. It will be committed to this integration branch as the executable bridge between the trajectory-level conformal score and exact affine capability half-spaces.
