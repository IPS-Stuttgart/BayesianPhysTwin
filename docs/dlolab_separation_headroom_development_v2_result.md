# DLO-Lab Separation Headroom Development v2 Result

The one registered v2 attempt is terminally **rejected at native
qualification**. World 1 completed ordinary native execution, sealed its full
public-simulator bundle, and passed nine of ten frozen checks. The material
attachment check failed: the maximum registered distance was `0.030451831 m`,
above the frozen `0.020 m` cap. Eight worlds were therefore never run and no
development value analysis occurred.

Standalone post-run arithmetic rehashed every content-addressed record and
array, reconstructed the native final reward to `5.8e-9 m`, confirmed exact
duplicate trajectories, and recomputed a final attachment distance of
`0.030451831 m` directly from node 27 of rope A and the first gripper target.
That reproduces the failed threshold without calling the production QA
function.

The remaining native checks passed, including finite execution, common prefix,
duplicate reward, registered rotation, segment length, rod height, and reward
reconstruction. This pattern suggests the frozen attachment observable or its
threshold is mismatched to the native end-effector target geometry; it does not
establish a general failure of DLO-Lab separation physics. The exact query is
nevertheless rejected because its preregistered qualification contract failed.

There is no retry, threshold adjustment, source transfer, prospective run, or
value claim. The unchanged best fixed action remains the fallback. Compact
evidence is in
`results/source/dlolab_separation_headroom_development_v2/summary.json`; the
write-once native root remains under
`/home/fpfaff/source-only/dlolab-separation-headroom-development-v2`.
