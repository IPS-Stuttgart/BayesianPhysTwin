# V14 reserve geometry child lock

This child lock binds the seven successful prefix-mask artifacts from the
fixed reserve batch before any reserve geometry is reconstructed. Queue rank
20 is retained as a source-preparation technical failure because its released
tactile stream has a zero normalization scale.

The reconstruction uses the unchanged official Deform360 prefix pipeline and
the unchanged V14 numerical settings. The child lock records the currently
relinked, source-identical gsplat extension directly instead of replaying the
historical runtime-amendment chain. Only object frames 0--57 may be consumed.
The known robot action remains separate and no source outcome, target artifact,
or held-v8 artifact is available to this stage.
