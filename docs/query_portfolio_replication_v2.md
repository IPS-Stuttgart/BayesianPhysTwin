# Prospective portfolio replication v2 recovery

The Slingshot v1 ledger-bearing attempt terminated during calibration-prefix
worker validation with zero ordinary prefix or future seals and no outcome
access. Its workers inherited the historical canonical library path from the
lock, which is intentionally not a filesystem path on the current host.

V2 retains the already running Wrapping v1 roster and every scientific and
statistical setting. It assigns entirely new Slingshot calibration, evaluation,
and sensor seeds. The sole implementation repair translates the canonical
library-path string to the byte-equivalent staged path in worker subprocesses;
workers independently recover the canonical identity before lock validation.
No v1 Slingshot world is retried or reused.
