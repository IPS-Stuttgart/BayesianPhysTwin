# Empirical evidence boundary

The implementation can be exercised retrospectively on previously opened DEFORM trajectories, but a primary finite-sample future-object claim requires a source-frozen object-disjoint calibration split and an untouched target-object split.
