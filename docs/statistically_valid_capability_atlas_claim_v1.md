# Claim template

Under exchangeability of complete calibration and target physical units, a split-conformal correction computed from one routed maximum score per calibration unit provides a finite-sample marginal bound for a future unit. When added to every exact model-side pairwise regret constraint, it yields a statistically corrected continuous task-capability atlas. The statement is marginal over future exchangeable units and conditional on the registered actions, task loss, routing procedure, and calibration protocol.
