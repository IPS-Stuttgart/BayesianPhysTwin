# Primary endpoint freeze

Only the following two v2 endpoints are primary:

1. equal-seed-domain mean full-sequence RMSE over the eleven stress conditions:
   `guarded_recursive - guarded_last_residual`;
2. equal-seed-domain total materially harmful accepted updates over the same
   conditions: `guarded_recursive - recursive_gaussian`.

Corruption-window RMSE, recovery-window RMSE, clean cost, imperfect-cue results,
physical-baseline contrast, guard-only contrast, calibration, width, fallback,
and worst-seed harm are registered secondary or co-equal companion evidence.
They cannot be relabelled primary after the fresh roster is opened.
