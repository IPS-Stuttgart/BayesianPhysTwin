# Statistical unit

The independent unit is the fresh seed-domain. Conditions, methods, and time
steps are paired or nested observations within seed-domain. Every interval first
reduces the registered conditions inside each seed and then resamples the 200
seed-domain values. Frames, forecasts, corruption steps, and condition-seed rows
are never treated as independent replicates.
