# Calibrated offset rule

For every exact action-region constraint `normal @ theta <= offset`, a nonnegative trajectory-conformal correction `q_plus` yields `normal @ theta <= offset - q_plus`. The operation preserves witness metadata and can only contract the capability atlas.
