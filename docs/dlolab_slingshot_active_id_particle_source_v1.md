# Slingshot Active-identification Full-particle Source Qualification

This is a prospective pre-truth qualification of the 70% front-loaded
diagnostic probe selected by the bounded development screen. The development
screen's retained serialization failure and post-result status remain explicit.
This study does not retry that output root.

The existing x-offset-zero 70% prefix histories are reused byte-for-byte. Four
new prefix-only CPU batches cover the two previously untested x-offset slices,
completing the 27-particle tensor prior. The passive prefix and seven-action
reward table remain the exact opened Slingshot model bank. No continuous truth
world, task future, or reward is generated.

Under 4,096 common bias/noise draws per particle world, the active probe must
improve expected Bayes reward by at least 0.005 over the best blind action and
0.003 over the passive prefix, capture at least 25% of finite-bank oracle
headroom, and have mutual information no lower than passive. All four new native
batches must pass. A pass authorizes only design of a separately frozen
continuous-world source study; it does not automatically execute one.

The exact write-once root is
`/home/fpfaff/source-only/dlolab-slingshot-active-id-particle-source-v1`, with
external attempt ledger
`/home/fpfaff/source-only/dlolab-slingshot-active-id-particle-source-v1.attempt.json`.
No protected target, held-v8 artifact, DLO4/DLO5 data, official DLO3 evaluation,
recording, GPU, truth future, or automatic promotion is permitted.
