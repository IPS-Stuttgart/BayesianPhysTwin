# Deform360 reusable association v2: source freeze

This milestone freezes an opt-in reusable material-association path before any
new calibration episode is opened. It does not modify the failed v1 artifact or
unseal any Deform360 target.

The source result covers five transferred actions of `081-stripe-rope` plus one
sheet and one volumetric source case. V2 retains source-reference appearance as
the identity criterion and uses calibrated 3D agreement as a feasibility gate.
Basic SAM2 candidates below the old hard appearance threshold may survive only
when the full multiview geometry passes. On three stripe-rope source actions,
that converts a strict-v1 candidate abort into 10--12 visually verified views.

The temporal path no longer assumes that equal-cardinality PLY exports are
infallible. Same-index warm-start pairs inside 30 mm receive distance-based
confidence; outliers are sparsely rematched or quarantined, and rematch
ambiguity is propagated as metric assignment variance. The five source
prefixes retain every identity with at least 97.09% effective support.

The pinned server run passes 511 tests with 5 skipped; all changed files pass
the focused Ruff checks.

The next gate is geometry-only on `081-stripe-rope` episodes 0, 2, and 8. All
three must pass the frozen camera, cardinality, and six-frame identity checks.
No future dynamics metric may be read at that gate.
