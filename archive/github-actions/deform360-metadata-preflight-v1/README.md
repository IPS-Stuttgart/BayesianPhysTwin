# Retired Deform360 metadata-only preflight workflow

The workflow formerly stored at
`.github/workflows/deform360-metadata-preflight.yml` is archived here byte for
byte. Its protocols, implementations, tests, documentation, historical runs,
and artifacts remain unchanged.

The metadata and fragment-eligibility phase completed its operational role. The
checked-in workflow slot is now used by the bounded four-object DLO
cross-episode preprocessing pilot. This retirement changes no prior dataset
inventory, eligibility result, model result, or scientific claim.
